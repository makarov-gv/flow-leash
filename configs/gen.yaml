base: base.yaml
data:
  latent_dir: runs/latents/ours
sit:
  preset: B
  patch_size: 2
  num_classes: 1000
  class_dropout: 0.1
train:
  batch_size: 64
  lr: 1.0e-4
  amp: true
  use_ema: true
  ema_decay: 0.9999
  t_dist: logitnormal
  max_steps: 100000
  log_every: 100
io:
  out_dir: runs/gen
